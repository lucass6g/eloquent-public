import { tv } from "tailwind-variants";

export  const labelVariants = tv({
    base: "text-sm text-foreground font-normal text-neutral-500 leading-4 peer-disabled:cursor-not-allowed peer-disabled:opacity-70",
})