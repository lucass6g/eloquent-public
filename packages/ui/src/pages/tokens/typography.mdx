import {Meta, Typeset,Source} from '@storybook/blocks'
import {FONT_FAMILY, FONT_SIZE, FONT_WEIGHT} from "@eloquent/tokens";

<Meta title="Tokens/Typography"/>

# Typography

**Font:** Inter, sans-serif

**Weights:**  300 (regular), 400 (regular), 500 (medium), 600 (semibold), 700 (bold), 800 (extrabold)

<Source
    type={'auto'}
    code={`
    import {Typography} from '@eloquent/ui'
    <Typography.Heading
        size="large"
        inverse
        as="h1"
    />
    `
}
/>
<Typeset
    fontSizes={[
        FONT_SIZE["3"],
        FONT_SIZE["3.5"],
        FONT_SIZE["4"],
        FONT_SIZE["5"],
        FONT_SIZE["6"],
        FONT_SIZE["8"],
        FONT_SIZE["10"],
        FONT_SIZE["12"],
        FONT_SIZE["14"],
        FONT_SIZE["16"],
        FONT_SIZE["20"]
    ]}
    sampleText={"Lorem  Ipsum Dolor Sit Amet"}
    fontWeight={Number(FONT_WEIGHT.regular)}
    fontFamily={FONT_FAMILY.sans}
/>


```
