import { Alert } from "@eloquent/ui";

export default function Example() {
  return (
    <Alert.Root>
        <Alert.Title>Alert Title</Alert.Title>
        <Alert.Description>
            Alert Description
        </Alert.Description>
    </Alert.Root>
  );
}
